<!DOCTYPE html>
<html lang="en">

{{>head}}

<body class="Site">
    <div class="Site-content">
        {{>header}}
        <p class="info">{{errorMessage}}</p>
    </div>
    {{>footer}}
</body>

</html>